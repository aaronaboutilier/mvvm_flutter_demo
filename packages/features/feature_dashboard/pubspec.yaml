name: feature_dashboard
description: Dashboard feature package (domain, application, and presentation layers).
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.9.2'
  flutter: ">=3.22.0"

dependencies:
  core_foundation:
    path: ../../core/core_foundation
  feature_settings:
    path: ../feature_settings
  flutter:
    sdk: flutter
  core_localization:
    path: ../../core/core_localization
  freezed_annotation: ^2.4.4
  get_it: ^8.0.0
  go_router: ^16.2.1
  provider: ^6.1.2

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  freezed: ^2.5.4
  lints: ^6.0.0
