import 'package:get_it/get_it.dart';

/// Registers Products feature dependencies into the provided GetIt locator.
/// Keep feature wiring self-contained; the app calls this during startup.
void registerFeatureProducts(GetIt locator) {
  // TODO: When migrating Products domain, register:
  // - repositories
  // - use cases
  // - view models
}
